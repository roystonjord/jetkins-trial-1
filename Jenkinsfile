pipeline {
  agent any
  stages {
    stage('Building'){
      steps {
        echo 'Building file
      }
    }
    stage('Running') {
      steps { 
        echo 'Running another command' 
      }
    }
  }
}
